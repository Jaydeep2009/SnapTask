package com.jaydeep.kaamly.data.model

/**
 * Enum representing user roles in the app
 */
enum class UserRole {
    USER,    // Can post tasks
    WORKER,  // Can complete tasks
    BOTH     // Can do both
}
